services:
  - type: web
    name: student-score-system
    runtime: docker
    # free 套餐不支持持久磁盘，服务休眠/重启后得分记录会丢失。
    # 需要永久保存得分时改为 plan: starter（约 $7/月）并加上：
    #   envVars:
    #     - key: DATA_DIR
    #       value: /data
    #   disk:
    #     name: data
    #     mountPath: /data
    #     sizeGB: 1
    plan: free
